{
  "name": "electron_puppeteer",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager . pupApp --platform win32 --arch x64 --out --overwrite dist/",
    "setup": "electron-installer-windows --src dist/app-win32-x64/ --dest dist/installers/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "asar": "^0.14.3",
    "electron": "^1.8.7",
    "electron-installer-squirrel-windows": "^1.3.0",
    "electron-packager": "^12.1.0"
  },
  "dependencies": {
    "jquery": "^3.3.1",
    "lodash": "^4.17.10",
    "puppeteer": "^1.6.0"
  }
}
